## 1. 实施前治理门禁

- [x] 1.1 在 `verification.md` 中记录 owner 对以下事项的审批：操作员权限矩阵、长期保留的显式项目接管、登录/资料响应的破坏性变更、迁移行为和发布顺序。
- [x] 1.2 盘点登录/资料 `project_roles`、租户请求头、项目成员加载、刷新令牌消费、用户修改和服务账号认证的全部调用方；实施前记录尚未覆盖的外部消费端。

## 2. 数据库与安全基础能力

- [x] 2.1 增加增量 Alembic revision：用户密码状态与锁定字段、刷新令牌 family 与原子消费字段、唯一服务账号项目 grant 表；为现有数据回填安全默认值。
- [x] 2.2 更新 SQLAlchemy 模型和 repository，支持登录失败状态、兼容密码状态字段、刷新令牌 family、原子消费与重放撤销、服务账号项目 grant。
- [x] 2.3 使用现有 PyJWT 依赖替换自定义 JWT 处理，并校验配置的算法、签发方、受众、类型、`exp`、`nbf`、`sub`、`jti` 和 `kid`，支持当前及旧验证密钥。
- [x] 2.4 增加迁移、JWT 校验、并发刷新、重放撤销 family、停用用户和账号锁定的聚焦测试；测试环境支持时验证 PostgreSQL 特有行为。

## 3. 本地密码身份流程

- [x] 3.1 调整管理员创建用户和重置凭据流程：只存密码哈希、写入 `must_change_password=false`，重置时仍撤销既有刷新令牌 family。
- [x] 3.2 实现“连续五次失败、锁定十五分钟”的本地账号锁定策略，公开错误保持统一，成功登录后清空失败计数。
- [x] 3.3 删除后端强制改密限制：已有 `must_change_password=true` 的活动用户也可正常登录、刷新并访问其权限允许的 API。
- [x] 3.4 更新登录、主动改密、重置、停用、刷新和退出测试，证明正常访问与会话撤销行为。

## 4. IAM 与单租户上下文

- [x] 4.1 在集中式 policy engine 中增加细粒度用户权限和平台项目权限及角色映射；保留最后一个超级管理员保护，并拒绝操作员修改超级管理员或分配角色。
- [x] 4.2 在服务端解析默认租户，授权、审计和转发均忽略客户端租户请求头，并更新可信请求上下文及测试。
- [x] 4.3 增加当前项目授权加载和 `/api/projects/{project_id}/access`；认证请求、登录响应和资料响应不再加载或返回完整项目角色映射。
- [x] 4.4 更新权限矩阵测试，覆盖超级管理员、操作员、只读者、项目管理员/编辑者/执行者、缺少项目范围，以及历史强制改密标记不限制正常授权。

## 5. 项目治理与隔离

- [x] 5.1 项目创建必须校验显式平台权限；项目创建在服务端默认租户中，并建立显式初始 `project_admin` 成员关系。
- [x] 5.2 增加全局项目元数据与生命周期治理、项目管理员恢复、要求填写原因的超级管理员显式接管；项目内容权限继续由成员关系控制。
- [x] 5.3 增加分页的项目成员候选人接口，使用 `project.member.write` 授权且不要求全局用户目录权限。
- [x] 5.4 审查本变更涉及的全部项目资源查询路径，在共享授权边界强制校验路由项目、当前项目和目标资源所属项目一致。
- [x] 5.5 增加集成测试，覆盖跨项目 ID 替换、非成员超级管理员内容访问拒绝、项目接管、无管理员项目恢复、候选人访问、项目创建拒绝和最后一个项目管理员保护。

## 6. 服务账号项目授权

- [x] 6.1 增加服务账号 grant 列表、新增或修改、删除契约和应用服务，使用超级管理员授权，并校验账号和项目处于活动状态。
- [x] 6.2 API key 授权只加载当前项目的服务账号 grant；grant 缺失或删除、账号停用、令牌过期或撤销、项目删除时拒绝访问。
- [x] 6.3 增加已授权访问、未授权访问、跨项目替换、删除 grant 无需轮换 key、无权限管理 grant 的测试。

## 7. 语义化 IAM 审计

- [x] 7.1 为用户创建、资料、状态、凭据、角色修改，项目生命周期、恢复、接管、成员修改，以及服务账号 grant 修改增加明确的审计 action 和 target 解析。
- [x] 7.2 将接管原因保存为非秘密 metadata；验证租户和项目归属来自服务端，同时确保密码、哈希、API key、刷新令牌、Cookie 和 Authorization 请求头不会写入审计。
- [x] 7.3 扩展审计解析和 HTTP 集成测试，覆盖 IAM 修改成功与拒绝场景。

## 8. Platform Web 集成

- [x] 8.1 更新前端契约、权限映射、工作区状态和路由：按当前项目加载授权，不再消费完整 `project_roles` 映射。
- [x] 8.2 更新登录和密码页面：登录后进入正常工作区，安全页保留主动改密，并继续显示统一的锁定/认证失败信息。
- [x] 8.3 更新用户创建和详情流程：分离资料、状态、平台角色分配和凭据重置，并增加权限 gate 与危险操作确认。
- [x] 8.4 更新项目列表、创建、详情和成员页面：支持平台项目治理、接管与恢复、项目范围候选人、当前项目授权刷新，以及 loading/empty/error/确认状态。
- [x] 8.5 更新服务账号详情页面：将项目 grant 与 API key 令牌分开管理，并增加权限 gate 和删除确认。
- [x] 8.6 更新聚焦 Vitest，覆盖权限映射、登录后正常路由、安全页主动改密、当前项目授权刷新、用户操作可见性、项目接管与恢复、候选人搜索和服务账号 grant 交互。

## 9. 验证与文档

- [x] 9.1 持续维护 `verification.md`，记录取消强制改密的 owner 决策、准确命令/输入/结果、迁移证据、未覆盖边界、残余风险、文档/runbook 影响和最终处置结论。
- [x] 9.2 先运行最小相关 `platform-api` 测试，再根据失败和共享边界影响运行所需的完整 API 测试集。
- [x] 9.3 运行聚焦 `platform-web` 测试，以及 lint、typecheck 和生产构建。
- [x] 9.4 验证最短正式链路：管理员创建用户后可直接正常登录，历史 `must_change_password=true` 不限制总览和项目列表，安全页可主动改密；同时复核项目 A/B 隔离、项目接管、服务账号项目访问和语义审计不受影响。
- [x] 9.5 使用代表性现有数据库验证 Alembic upgrade，并记录保留增量结构的非破坏性应用回滚方式。
- [x] 9.6 更新项目手册、运行手册及其他受影响文档，删除首次登录强制改密说明，并确认权限、审计、迁移和回滚文档仍准确。
- [x] 9.7 运行 `graphify update .`、记录最终证据、取得 owner 验收、同步已接受的 delta specs 并归档；未经明确授权不得执行 commit 或 push。
